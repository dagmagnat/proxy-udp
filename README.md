
```bash
bash <(wget -qO- --inet4-only https://raw.githubusercontent.com/dagmagnat/proxy-udp/main/proxy-udp)
```
```bash
bash <(wget -qO- --inet4-only https://raw.githubusercontent.com/dagmagnat/proxy-udp/main/mtproto-manager)
```

